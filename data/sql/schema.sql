CREATE TABLE image (id BIGINT AUTO_INCREMENT, url VARCHAR(255) NOT NULL, params VARCHAR(255), hash VARCHAR(255) NOT NULL UNIQUE, path VARCHAR(255) NOT NULL, accessed_at DATETIME NOT NULL, processed_at DATETIME, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
