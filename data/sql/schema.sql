CREATE TABLE render_job (id BIGINT AUTO_INCREMENT, params text, hash VARCHAR(255) NOT NULL UNIQUE, path VARCHAR(255) NOT NULL, accessed_at DATETIME NOT NULL, process_started_at DATETIME, process_finished_at DATETIME, type VARCHAR(255), created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
CREATE TABLE render_job (id BIGINT AUTO_INCREMENT, params text, hash VARCHAR(255) NOT NULL UNIQUE, path VARCHAR(255) NOT NULL, accessed_at DATETIME NOT NULL, process_started_at DATETIME, process_finished_at DATETIME, type VARCHAR(255), created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, INDEX render_job_type_idx (type), PRIMARY KEY(id)) ENGINE = INNODB;
